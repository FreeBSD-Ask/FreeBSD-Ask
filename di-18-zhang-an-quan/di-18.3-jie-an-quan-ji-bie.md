# 18.3 安全级别

## 课后习题

1. 将 `kern.securelevel` 依次设置为 0、1、2，在每个级别下尝试以下操作：删除一个 `schg` 标志的文件、加载一个内核模块、修改 `kern.securelevel` 本身。列表汇总各操作在各安全级别下的允许/拒绝情况。

2. 解释为什么 `kern.securelevel` 的数值只能增加而不能减少？从攻击者的角度分析，如果允许降低安全级别会引发哪些安全威胁场景？

3. 有说法认为这种安全级别在现代计算机安全中不再重要，为什么？
