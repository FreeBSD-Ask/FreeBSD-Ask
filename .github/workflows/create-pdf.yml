name: 🔗 创建并发布 PDF

on:
  schedule:
    - cron: '0 0 */5 * *'  # 每隔5天运行一次
  workflow_dispatch:  # 允许手动触发

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout ttf-mswin11-zh-deb
        uses: actions/checkout@v4
        with:
          repository: ykla/ttf-mswin11-zh-deb
          
      - name: Install font dependencies
        run: |
          sudo apt update
          sudo apt install -y xfonts-encodings xfonts-utils 
          sudo dpkg -i ttf-ms-win11-*.deb

      - name: Checkout FreeBSD-Ask repository
        uses: actions/checkout@v4
        with:
          path: FreeBSD-Ask
        
      - name: Checkout gitbook-pdf-export repository
        uses: actions/checkout@v4
        with:
          repository: FreeBSD-Ask/gitbook-pdf-export
          path: gitbook-pdf-export

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Install PDF generation dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libpango-1.0-0 libpangoft2-1.0-0 libpangocairo-1.0-0
          pip install weasyprint mistune pygments

      - name: Generate PDF
        run: |
          cd gitbook-pdf-export
          python mdconv.py ../FreeBSD-Ask
        env:
          PYTHONPATH: ${{ github.workspace }}/gitbook-pdf-export

      - name: Install Changelog Generator
        run: sudo gem install github_changelog_generator

      - name: Generate Changelog
        run: |
          github_changelog_generator -u FreeBSD-Ask -p FreeBSD-Ask --token ${{ secrets.GITHUB_TOKEN }} \
            --pull-requests --issues=false --no-unreleased
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Set release tag
        id: set_tag
        run: echo "RELEASE_TAG=$(date +'%Y.%m.%d')" >> $GITHUB_ENV

      - name: Create Release
        uses: ncipollo/release-action@v1
        with:
          tag: ${{ env.RELEASE_TAG }}
          name: ${{ env.RELEASE_TAG }}
          artifacts: gitbook-pdf-export/build/final.pdf
          body_file: CHANGELOG.md
          token: ${{ secrets.GITHUB_TOKEN }}
