# 13.2 大模型本地部署

## misc/llama-cpp

llama.cpp 致力于降低门槛，旨在通过最简化的配置，在各类硬件平台上实现极致的大模型推理性能。其主要使用 C/C++ 写就。

### 安装

- 使用 pkg 安装：

```sh
# pkg install llama-cpp
```

- 使用 Ports 安装：

```sh
# cd /usr/ports/misc/llama-cpp/
# make install clean
```

## misc/ollama

### 安装

- 使用 pkg 安装：

```sh
# pkg install ollama
```

- 使用 Ports 安装：

```sh
# cd /usr/ports/misc/ollama/
# make install clean
```

## misc/claude-code

### 安装

- 使用 pkg 安装：

```sh
# pkg install claude-code
```

- 使用 Ports 安装：

```sh
# cd /usr/ports/misc/claude-code/
# make install clean
```

## misc/github-copilot-cli

### 安装

- 使用 pkg 安装：

```sh
# pkg install github-copilot-cli
```

- 使用 Ports 安装：

```sh
# cd /usr/ports/misc/github-copilot-cli/
# make install clean
```